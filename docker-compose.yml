services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: almacen
      POSTGRES_PASSWORD: almacen_dev
      POSTGRES_DB: almacen
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
